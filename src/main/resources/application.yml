spring:
  profiles:
    active:
      - local

---

spring:
  config:
    activate:
      on-profile:
        - local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/order_service
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: none

server:
  port: 8081
